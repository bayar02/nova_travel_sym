{% extends 'base.html.twig' %}

{% block title %}Reclamation index{% endblock %}

{% block stylesheets %}
    <style>
        .container-fluid-custom {
            padding: 40px 60px;
        }

        h1 {
            text-align: center;
            color: #333;
            margin-bottom: 30px;
        }

        .card {
            background-color: #fff;
            padding: 20px;
            border-radius: 12px;
            box-shadow: 0 2px 12px rgba(0, 0, 0, 0.1);
        }

        .table-wrapper {
            overflow-x: auto;
        }

        table {
            width: 100%;
            border-collapse: separate;
            border-spacing: 0 10px;
            margin-top: 10px;
            min-width: 900px;
        }

        thead th {
            background-color: #f3f3f3;
            padding: 12px 16px;
            text-align: left;
            border-radius: 8px;
        }

        tbody tr {
            background-color: #fafafa;
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }

        tbody tr:hover {
            transform: scale(1.005);
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.08);
        }

        tbody td {
            padding: 12px 16px;
            border-top: 1px solid #eee;
        }

        .actions a {
            margin-right: 10px;
            color: #007bff;
            text-decoration: none;
        }

        .actions a:hover {
            text-decoration: underline;
        }

        .create-button {
            margin-top: 20px;
            display: inline-block;
            padding: 10px 15px;
            background-color: #28a745;
            color: #fff;
            border-radius: 5px;
            text-decoration: none;
        }

                .response-button {
            margin-top: 20px;
            display: inline-block;
            padding: 10px 15px;
            background-color:rgb(70, 101, 213);
            color: #fff;
            border-radius: 5px;
            text-decoration: none;
        }

        .create-button:hover {
            background-color: #218838;
        }
    </style>
{% endblock %}


{% block body %}
    <div class="container-fluid container-fluid-custom">
            <a href="{{ path('app_reclamation_new') }}" class="create-button">
                 <i class='bx bx-plus'></i> Créer une nouvelle réclamation
            </a>
        <a href="{{ path('app_reponse_index') }}" class="response-button">Liste des Réponses </a>

        <h1>Liste des Réclamations</h1>
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="table-wrapper">
                    <table>
                        <thead>
                            <tr>
                                <th>Id</th>
                                <th>Date</th>
                                <th>Type</th>
                                <th>Message</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for reclamation in reclamations %}
                                <tr>
                                    <td>{{ reclamation.id }}</td>
                                    <td>{{ reclamation.dateReclamation|date('Y-m-d') }}</td>
                                    <td>{{ reclamation.type }}</td>
                                    <td>{{ reclamation.message }}</td>
                                    <td class="actions">
                                        <a  href="{{ path('app_reclamation_show', {'id': reclamation.id}) }}">
                                            <i class='bx bx-show'></i> Show
                                        </a>
                                        <a href="{{ path('app_reclamation_edit', {'id': reclamation.id}) }}">
                                            <i class='bx bx-edit'></i> Edit
                                        </a>
                                        <a href="{{ path('reclamation_pdf', {'id': reclamation.id}) }}" target="_blank" class="btn btn-primary">
    Télécharger le PDF
</a>
                                    </td>
                                </tr>
                            {% else %}
                                <tr>
                                    <td colspan="5" style="text-align: center;">Aucune réclamation trouvée.</td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
                <div class="pagination-wrapper">
                    {{ knp_pagination_render(reclamations) }}
                </div>
            </div>
        </div>


    </div>
{% endblock %}

