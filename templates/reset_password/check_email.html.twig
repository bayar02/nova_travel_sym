{% extends 'base.html.twig' %}

{% block title %}Password Reset Email Sent{% endblock %}

{% block body %}
<div class="container mt-5" style="min-height: 80vh; display: flex; align-items: center; justify-content: center;">
    <div class="card p-4 shadow" style="max-width: 500px; width: 100%; text-align: center;">
        <h2 class="mb-4">📧 Check your email</h2>

        <p>
            If an account matching your email exists, we have sent a password reset link.
            <br>
            The link will expire in <strong>{{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}</strong>.
        </p>

        <p class="mt-3 text-muted">
            If you don't receive an email, please check your spam folder<br> or
            <a href="{{ path('app_forgot_password_request') }}">try again</a>.
        </p>
    </div>
</div>
{% endblock %}
