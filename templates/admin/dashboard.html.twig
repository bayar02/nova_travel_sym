{% extends 'admin/base_admin.html.twig' %}

{% block admin_title %}Admin Dashboard{% endblock %}

{% block admin_stylesheets %}
    {# Add specific dashboard styles if needed #}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; color: #333 }
    </style>
{% endblock %}

{% block admin_content %}
    {# Removed sidebar HTML - it's now in base_admin.html.twig #}

    {# Original Content #}
    {# Removed outer admin-content-wrapper, now handled by base #}
    <div class="example-wrapper">
        <h1>Admin Dashboard ✅</h1>
        Welcome, Admin {{ app.user.userIdentifier }}!<br>
        This is the admin area.
    </div>
{% endblock %}
