{% extends 'base.html.twig' %}

{% block title %}Hebergement index{% endblock %}

<<<<<<< HEAD
{% block body %}
    <h1>Hebergement index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Type</th>
                <th>Nom</th>
                <th>Adresse</th>
                <th>Description</th>
                <th>Prix_nuit</th>
                <th>Photo</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for hebergement in hebergements %}
            <tr>
                <td>{{ hebergement.id }}</td>
                <td>{{ hebergement.type }}</td>
                <td>{{ hebergement.nom }}</td>
                <td>{{ hebergement.adresse }}</td>
                <td>{{ hebergement.description }}</td>
                <td>{{ hebergement.prixNuit }}</td>
                <td>{{ hebergement.photo }}</td>
                <td>
                    <a href="{{ path('app_hebergement_show', {'id': hebergement.id}) }}">show</a>
                    <a href="{{ path('app_hebergement_edit', {'id': hebergement.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_hebergement_new') }}">Create new</a>
=======
{% block stylesheets %}
    <style>
        .container-fluid-custom {
            padding: 40px 60px;
        }

        h1 {
            text-align: center;
            color: #333;
            margin-bottom: 30px;
        }

        .card {
            background-color: #fff;
            padding: 20px;
            border-radius: 12px;
            box-shadow: 0 2px 12px rgba(0, 0, 0, 0.1);
        }

        .table-wrapper {
            overflow-x: auto;
        }

        table {
            width: 100%;
            border-collapse: separate;
            border-spacing: 0 10px;
            margin-top: 10px;
            min-width: 900px;
        }

        thead th {
            background-color: #f3f3f3;
            padding: 12px 16px;
            text-align: left;
            border-radius: 8px;
        }

        tbody tr {
            background-color: #fafafa;
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }

        tbody tr:hover {
            transform: scale(1.005);
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.08);
        }

        tbody td {
            padding: 12px 16px;
            border-top: 1px solid #eee;
        }

        .actions a {
            margin-right: 10px;
            color: #007bff;
            text-decoration: none;
        }

        .actions a:hover {
            text-decoration: underline;
        }

        .create-button {
            margin-top: 20px;
            display: inline-block;
            padding: 10px 15px;
            background-color: #28a745;
            color: #fff;
            border-radius: 5px;
            text-decoration: none;
        }

                .reservation-button {
            margin-top: 20px;
            display: inline-block;
            padding: 10px 15px;
            background-color:rgb(70, 101, 213);
            color: #fff;
            border-radius: 5px;
            text-decoration: none;
        }

        .create-button:hover {
            background-color: #218838;
        }
    </style>
{% endblock %}

{% block body %}
    <div class="container-fluid container-fluid-custom">
        <a href="{{ path('app_hebergement_new') }}" class="create-button">
            <i class='bx bx-plus'></i> Créer un nouvel hébergement
        </a>
         <a href="{{ path('app_reservation_hebergement_index') }}" class="reservation-button">Liste des Reservations </a>

        <h1>Liste des Hébergements</h1>

        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="table-wrapper">
                    <table>
                        <thead>
                            <tr>
                                <th>Photo</th>
                                <th>Id</th>
                                <th>Type</th>
                                <th>Nom</th>
                                <th>Adresse</th>
                                <th>Description</th>
                                <th>Prix/nuit</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for hebergement in hebergements %}
                                <tr>
                                    <td>
                                        {% if hebergement.photo %}
                                            <img src="{{ asset('uploads/' ~ hebergement.photo) }}" alt="photo" width="80">
                                        {% else %}
                                            N/A
                                        {% endif %}
                                    </td>
                                    <td>{{ hebergement.id }}</td>
                                    <td>{{ hebergement.type }}</td>
                                    <td>{{ hebergement.nom }}</td>
                                    <td>{{ hebergement.adresse }}</td>
                                    <td>{{ hebergement.description }}</td>
                                    <td>{{ hebergement.prixNuit }}</td>
                                    <td class="actions">
                                        <a href="{{ path('app_hebergement_show', {'id': hebergement.id}) }}">
                                            <i class='bx bx-show'></i> Show
                                        </a>
                                        <a href="{{ path('app_hebergement_edit', {'id': hebergement.id}) }}">
                                            <i class='bx bx-edit'></i> Edit
                                        </a>
                                    </td>
                                </tr>
                            {% else %}
                                <tr>
                                    <td colspan="8" style="text-align: center;">Aucun hébergement trouvé.</td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
>>>>>>> f5842df (Initial commit for Events branch)
{% endblock %}
