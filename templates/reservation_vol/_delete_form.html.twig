<form method="post" action="{{ path('app_reservation_vol_delete', {'id': reservation_vol.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation_vol.id) }}">
    <button class="btn">Delete</button>
</form>
