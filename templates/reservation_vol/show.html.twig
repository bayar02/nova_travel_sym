{% extends 'base.html.twig' %}

{% block title %}ReservationVol{% endblock %}

{% block body %}
    <h1>ReservationVol</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservation_vol.id }}</td>
            </tr>
            <tr>
                <th>Classe</th>
                <td>{{ reservation_vol.classe }}</td>
            </tr>
            <tr>
                <th>Nb_billets</th>
                <td>{{ reservation_vol.nbBillets }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reservation_vol_index') }}">back to list</a>

    <a href="{{ path('app_reservation_vol_edit', {'id': reservation_vol.id}) }}">edit</a>

    {{ include('reservation_vol/_delete_form.html.twig') }}
{% endblock %}
